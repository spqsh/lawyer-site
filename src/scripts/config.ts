export const config = {
  scrollSpeed: 18,
};

export interface IDynamicNavigationLink {
  block: HTMLElement;
  link: HTMLElement;
}

export interface ISmoothScrollItems {
  block: HTMLElement;
  links: HTMLElement[];
}
